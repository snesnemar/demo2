server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reward_system?useSSL=false&serverTimezone=Asia/Taipei
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: wednesday3work@gmail.com
    password: "ohav rxcf wbfs zbwa"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
    default-encoding: UTF-8

  security:
    user:
      name: user
      password: password
